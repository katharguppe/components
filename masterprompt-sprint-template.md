# Master Prompt — Sprint XX: [FILL: Theme Name]

<!--
  HOW TO USE THIS TEMPLATE
  ========================
  This file is generated by new-sprint.ps1.
  Fill every [FILL: ...] section before starting the Claude Code session.
  The more specific you are here, the less back-and-forth inside Claude Code.

  CHECKLIST before opening Claude Code:
  [ ] Sprint number filled in header
  [ ] Checkpoint file name filled in (check last existing CHECKPOINT_XX.md)
  [ ] Deliverables table filled in (filenames, not vague descriptions)
  [ ] Key Facts filled in (schema decisions, route patterns, column types)
  [ ] Known Gotchas filled in (anything that burned you in prior sprints)
  [ ] Regression gate confirmed (run test-e2e.js locally to verify baseline)
  [ ] Definition of Done filled in (concrete, checkable items)
-->

## Context

```
Project  : Travel SaaS — Multi-Tenant Authentication (saas-auth)
Root     : D:\vaikunta-ekadashi\Components\saas-auth
Sprint   : XX — [FILL: Theme Name]
Status   : IN PROGRESS
Checkpoint: [FILL: CHECKPOINT_NN.md — the NEW checkpoint for this sprint]
Prior checkpoint: [FILL: CHECKPOINT_MM.md — last completed sprint checkpoint]
```

Stack: Node.js 20, TypeScript, Express, Prisma ORM (raw SQL for tenant schemas),
PostgreSQL 15 (Docker), RS256 JWT, Zod validation, Argon2id, Docker Compose.

## Resume Instructions

1. Read `[FILL: CHECKPOINT_NN.md]` — understand what is planned and what is complete
2. Run `node test-e2e.js` — all prior sprint tests must be green before starting
3. State current status. Propose plan. Wait for approval. Then implement.
4. PDCA: one cycle per deliverable. Check tests after each file. No batch commits.

## This Sprint's Deliverables

<!--
  List every file you expect to CREATE or MODIFY.
  Be specific — filename + purpose. Blank rows are OK if unknown yet.
  Claude will be held to this scope. Anything outside needs explicit approval.
-->

| # | File | Action | Purpose |
|---|------|--------|---------|
| 1 | `[FILL: path/to/file.ts]` | CREATE | [FILL: what it does] |
| 2 | `[FILL: path/to/file.ts]` | MODIFY | [FILL: what changes] |
| 3 | `[FILL: db/migrations/...]` | CREATE | [FILL: tables/columns] |
| 4 | `[FILL: test-sprintXX-routes.js]` | CREATE | [FILL: N tests] |
| 5 | `CHECKPOINT_NN.md` | CREATE | Sprint plan + resume point |
| 6 | `README_FULL.md` | MODIFY | Append Sprint XX endpoint table |

## Scope — DO NOT TOUCH outside this list

```
IN SCOPE:
  [FILL: list files/folders]

OUT OF SCOPE (do not touch):
  packages/auth-bff/src/routes/auth.routes.ts     ← Sprint 01, complete
  packages/auth-bff/src/routes/admin.routes.ts    ← Sprint 02, complete
  packages/auth-bff/src/routes/operator.routes.ts ← Sprint 02, complete
  packages/auth-bff/src/routes/client.routes.ts   ← Sprint 03, complete
  packages/auth-bff/src/routes/group.routes.ts    ← Sprint 03, complete
  [FILL: add any other locked files]
```

## Key Facts

<!--
  The most important technical decisions for this sprint.
  Examples: column types, route prefixes, auth chain, business rules.
  Be specific — vague facts lead to wrong implementations.
-->

- **Route prefix**: [FILL: e.g. `/api/v1/bookings`]
- **Auth chain**: `requireAuth → requireTenant → requireRole('admin','operator')`
- **Tenant scope**: resolved from `X-Tenant-Slug` header + JWT claim
- **[FILL: key schema decision]**: [FILL: detail]
- **[FILL: key business rule]**: [FILL: detail]
- **[FILL: primary key type]**: [FILL: e.g. UUID with ::uuid cast in raw Prisma queries]
- **Soft deletes**: [FILL: column name and value, e.g. `status = 'cancelled'`]
- **Idempotency**: [FILL: which operations must be safe to repeat]

## API Contract

<!--
  List every new endpoint. Fill this before implementation — it is the contract.
-->

| Method | Path | Auth | Body / Query | Response |
|--------|------|------|-------------|----------|
| [FILL] | [FILL] | admin/operator | [FILL] | [FILL] |
| [FILL] | [FILL] | admin/operator | [FILL] | [FILL] |

## Database Schema

<!--
  List new tables or columns. Specify types precisely — avoids UUID cast bugs.
-->

```sql
-- [FILL: table name]
-- Schema: tenant_{slug}.[FILL: table_name]
CREATE TABLE IF NOT EXISTS {schema}.[FILL] (
  id          UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  -- [FILL: other columns]
  created_at  TIMESTAMPTZ DEFAULT now(),
  updated_at  TIMESTAMPTZ DEFAULT now()
);
```

## Seeded Accounts

| Role | Email | Password | Tenant slug |
|------|-------|----------|-------------|
| operator | operator@yoursaas.com | Operator@Secure123! | system |
| admin | admin@acme.com | Admin@Acme123! | acme-corp |
| user | alice@acme.com | User@Acme123! | acme-corp |
| admin | admin@betaorg.com | Admin@Beta123! | beta-org |

## Known Gotchas (carry forward + new)

- **Prisma raw + UUID**: any `uuid`-typed column needs `$1::uuid` cast — Prisma sends all params as `text`
- **Route mount order**: static segments before wildcard segments in `app.ts`
- **Preferences body pattern**: nested `{ preferences: { ... } }` not flat
- **Login body**: `tenant_slug` must be in the request body for `/auth/login`
- **BUG-001**: `requireSameTenant` not wired — cross-tenant access is not blocked (backlog)
- **[FILL: sprint-specific gotcha]**: [FILL: detail]

## Regression Gate

```bash
# Must all pass BEFORE committing any Sprint XX code
node test-e2e.js              # 69+ tests — all prior sprints green

# Sprint XX specific (write this before implementing):
node test-sprint[XX]-routes.js   # [FILL: N]/[FILL: N] tests
```

## Definition of Done

- [ ] `CHECKPOINT_NN.md` written and committed
- [ ] All new source files written and type-checking clean
- [ ] `test-sprintXX-routes.js` — [FILL: N]/[FILL: N] passing
- [ ] `node test-e2e.js` — 69+ passing, 0 regressions
- [ ] `README_FULL.md` — Sprint XX endpoint table appended
- [ ] Git committed with `[SPRINT-XX]` message

## Git Format

```
[SPRINT-XX] feat|fix|test|docs: what changed

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```
